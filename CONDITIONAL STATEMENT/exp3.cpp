#include<iostream>
using namespace std;
int main ()
{
    int a;
    cout << "Enter a number" << endl;
    cin >> a;
    if (a>0)
    {
        cout << "Entred number is positive" <<endl;
    }
        else if(a<0)
        {
            cout << "Entred number is negative" << endl;
        }
        else
        cout << "Entred number is Zero" << endl;
        return 0; 
        
    
    
    return 0;
}